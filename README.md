# ArenaOS

Single-operator autonomous AI platform. The agent thinks, plans, runs real
tools (shell, files, network, git) inside a Linux-jail sandbox, and answers
chat through a **live arena.ai browser session** — your account, every model on
the arena, no API keys, no added content filters. The platform enforces real
security boundaries only (jail, secrets, permissions, audit).

## Stack

- **FastAPI + SQLAlchemy** (SQLite by default, Postgres-ready via `DB_URL`)
- **Playwright headless Chromium** driving the real arena.ai chat UI with a
  persistent logged-in profile (selectors verified against the live site)
- **Linux sandbox**: per-project workspaces, rlimits (CPU/RAM/PIDs), process
  groups, real exit codes; background process table
- **Real tools**: shell, fs (jailed), HTTP, keyless web search (DuckDuckGo), git
  (push permission-gated)
- **Task engine**: persisted state machine — plan → execute → checkpoint →
  retry (backoff) → self-debug → resume; permission blocks with approve flow
- **Secret vault**: Fernet-encrypted credentials, redaction on every log/audit
  surface; values are never echoed back
- **UI**: consumer chat aesthetic — centered greeting, pill input (+ / mic),
  drawer with recents & library, streaming replies, Thoughts panel

## Deploy on Render (free tier)

1. Push this repo to GitHub.
2. Render dashboard → **New → Blueprint** → select the repo (uses `render.yaml`).
3. Set the generated secrets in Render's dashboard:
   - `OPERATOR_PASSWORD` — your platform login password
   - `SECRET_VAULT_KEY` / `JWT_SECRET` — auto-generated by the blueprint
4. Open the service URL, log in with your operator password.

### First run: connect your arena.ai account

Go to **Settings → Vault** and add two credentials (values are encrypted and
never shown back):

- `arena_web_email` (kind: `email`) — your arena.ai login email
- `arena_web_password` (kind: `password`) — your arena.ai login password

The app logs into arena.ai at startup using a persistent browser profile, so
you stay logged in across restarts. Settings → status chip shows the honest
session state (`ready` / `login_required` / `captcha_required`).

**Render free tier = 512 MB.** The browser launches only when a chat message is
sent, so idle RAM stays low. If the browser transport gets tight, either bump
to the $7 Starter (no code change) or keep `ARENA_TRANSPORT=api` and configure
an OpenAI-compatible endpoint instead.

### Keep it awake 24/7 (free)

Render sleeps idle web services after 15 minutes. Create a free monitor at
https://uptimerobot.com pointing at `https://<your-service>.onrender.com/healthz`
every 5 minutes. This keeps the app — and the arena.ai session cookies — warm.

## Local quick start

```bash
pip install -e '.[browser,postgres]'   # or plain '.[browser]' for SQLite
python -m playwright install chromium
export OPERATOR_PASSWORD=change-me
uvicorn arenaos.api.app:app --port 8000
# open http://localhost:8000
```

## Tests

```bash
python -m pytest tests/ -q
```

38 tests cover security/redaction, the vault, the sandbox (real executions,
timeout kills, jail escapes), tools (shell/fs/git/HTTP/search), memory, the
engine lifecycle (retry, debug, permission-approve, cancel), and the API
(auth, files jail, vault value-hiding, honest degradation).

Browser-transport tests are skipped when Chromium isn't installed — they run
in the Docker image where `python -m playwright install chromium` executes at
build time.

## Honest limits

- The arena.ai web transport is a real browser session — if arena.ai raises a
  reCAPTCHA for anonymous/datacenter sessions, the platform reports
  `captcha_required` instead of hanging or faking. Logging in with your real
  account avoids nearly all of these.
- The Linux sandbox is a process-level jail with rlimits (safe for Render's
  shared free tier). For hard multi-tenant isolation, run on a dedicated
  host/container — the workspace root is configurable.
